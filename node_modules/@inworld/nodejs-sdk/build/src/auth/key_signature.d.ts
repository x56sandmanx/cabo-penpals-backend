import { ApiKey } from '../common/data_structures';
export interface KeySignatureProps {
    host: string;
    apiKey: ApiKey;
}
export declare class KeySignature {
    static getAuthorization({ host, apiKey }: KeySignatureProps): string;
    static getSignatureKey(key: string, params: string[]): string;
    private static getDateTime;
}
