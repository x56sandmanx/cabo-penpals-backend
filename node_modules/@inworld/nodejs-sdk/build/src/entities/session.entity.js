"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Session = void 0;
const session_token_entity_1 = require("./session_token.entity");
class Session {
    constructor(props) {
        this.sessionToken = props.sessionToken;
        this.scene = props.scene;
    }
    static serialize(session) {
        return JSON.stringify(session);
    }
    static deserialize(json) {
        try {
            const { sessionToken, scene } = JSON.parse(json);
            return new Session({
                sessionToken: new session_token_entity_1.SessionToken({
                    token: sessionToken.token,
                    type: sessionToken.type,
                    sessionId: sessionToken.sessionId,
                    expirationTime: new Date(sessionToken.expirationTime),
                }),
                scene,
            });
        }
        catch (e) { }
    }
}
exports.Session = Session;
